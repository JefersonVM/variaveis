const primeiroNome = "Jeferson";
let segundoNome = "Vieira";
let idade = 42;

console.log(idade);

let alturaEmMetros = 1.74;

console.log(alturaEmMetros);

//string template

console.log(`Meu nome é ${primeiroNome} ${segundoNome} eu tenho ${idade} anos. Minha altura é ${alturaEmMetros} metros.`);

// A diferença entre var e let é que let é uma variável de escopo de bloco, enquanto var é uma variável de escopo de função.
// A variavel var pode ser utilizada antes de ser declarada, já a variavel let não pode ser utilizada antes de ser declarada.